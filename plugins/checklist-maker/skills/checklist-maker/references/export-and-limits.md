# 受け渡しと出力、そして制約

AI が書いたデータを、ユーザーがどうやって手順書（Word / PDF / Excel）にするか。
そして**このツールでは無理なこと**。

---

## 1. ユーザーへの受け渡し手順（これをそのまま案内する）

1. `json-to-html.mjs` で **取り込み用 HTML** を作り、それをユーザーに渡す
   （`.checklist.json` も一緒に渡してよいが、取り込むのは HTML のほう）
2. CheckListMaker を起動する（インストール済みのデスクトップ版）
3. ホーム画面の **「インポート」** ボタンから、その `.html` を選ぶ
4. 取り込まれた手順書を開き、必要なら画像を貼る／文言を直す
5. エディタ上部の **「ファイルで出力 ▾」** を開き、
   **「Word (.docx)」「Excel (.xlsx)」「PDF」** を選ぶ

> UI のボタン名（実装どおり）— ホーム画面: `インポート` / `エクスポート`。
> 上部バー: `元に戻す` / `やり直す` / `キャプチャ撮影` / `画像インポート`。
> エディタのメニューバー（横一列）:
> `ファイルで出力 ▾` / `印刷` / `作業を開始`（小窓が使える環境は `作業を開始 ▾`） /
> `作業をリセット` / `フェーズを追加` / `表紙を編集`。
> `ファイルで出力 ▾` の中身は `名前を付けて保存（HTML）` / `Word (.docx)` /
> `Excel (.xlsx)` / `PDF` / `共有リンク`。
> **メニューバーのボタンに絵文字は付かない**（2026-09-09 の横一列化以降。
> 上部バーのアイコンは残っている）。各ボタンはカーソルを合わせると解説が出る。

## 2. HTML と JSON、どちらで渡すか

| 形式 | 取り込みの挙動 | 使いどころ |
|---|---|---|
| **HTML**（推奨） | **同一 `id` は更新、無ければ追加＝マージ。確認なしで安全に入る** | 既定。これを渡す |
| JSON | 取り込み時に **「追加・更新する」／「すべて置き換える」** を選ばせるダイアログが出る | バックアップを丸ごと差し替えたいとき |

HTML 取り込みが安全なのは、アプリ側が「同一 `id` なら置換、無ければ追加」で処理するから。
`json-to-html.mjs` が出す HTML は `<script type="application/json" id="clm-data">` を持ち、
アプリの `parseChecklistFromHtml` がそれを読む。

### JSON を渡す場合の注意

取り込みダイアログで **「追加・更新する」** を選べば既存データは消えない。
**「すべて置き換える」** を選ぶと、今アプリに入っているチェックリストは**すべて破棄される**。
JSON を渡すときは、どちらを選ぶべきかを必ず明記すること。

> この JSON を読み込むときは、ダイアログで「追加・更新する」を選んでください。
> 「すべて置き換える」を選ぶと、今アプリに入っているデータはすべて消えます。

### 取り込み用 HTML は「自己完結HTML」ではない

`json-to-html.mjs` が出す HTML は、ブラウザで開けば手順書として**読める**が、
**編集はできない**（アプリ本体を埋め込んでいないため）。編集できる HTML が要るなら、
アプリで取り込んでから「ファイルで出力 ▾」→「名前を付けて保存（HTML）」を使う。

---

## 2. 出力できる形式

| 出力 | どこから | 中身 |
|---|---|---|
| **Word (.docx)** | ファイルで出力 ▾ →「Word (.docx)」 | 表紙（画像化）＋目次＋手順カード様式。游ゴシック。表紙・目次はページ番号から除外 |
| **PDF** | ファイルで出力 ▾ →「PDF」 | 印刷ビューをそのまま PDF 化。Word と同じ手順カード様式。**用紙サイズ・向き・倍率は「印刷」のプレビューで設定した値が使われる**（下記） |
| **Excel (.xlsx)** | ファイルで出力 ▾ →「Excel (.xlsx)」 | 列は `No / 項目 / チェック / 標準時間(分) / メモ / 詳細`。画像は手順の下に行を挿して埋め込み。表紙シート付き |
| **自己完結 HTML** | ファイルで出力 ▾ →「名前を付けて保存（HTML）」 | 単体で開いてフル編集できる HTML（画像込み）。AI が生成する取り込み用 HTML とは別物 |
| **JSON** | ホーム画面「エクスポート」 | state 全体。AI に渡し直すときはこれ |
| **共有リンク** | ファイルで出力 ▾ →「共有リンク」 | URL に埋め込み。**画像は落ちる** |
| ~~CSV~~ | — | 出力処理（BOM 付き UTF-8・CRLF）は実装済みだが、**現在の UI にボタンが無く呼び出せない**。ユーザーに CSV を約束しないこと |

### 用紙・向き・倍率（印刷プレビュー）

メニューバーの **「印刷」** で印刷プレビューが開き、そこで **用紙サイズ**（A4 / A3 / B4 /
B5 / Letter）・**向き**（縦 / 横）・**倍率（30〜200%）** を指定できる。設定は保存され、
**「PDF」出力にもそのまま効く**（ブラウザの印刷は OS のダイアログ側で倍率を指定する）。

- プレビューには **赤い破線で改ページの目安**とページ番号が出る。図や表がページを
  またぐときの送りまでは再現しないので、あくまで目安。
- 倍率は**画像にも効く**。ワイド画像を1ページに複数入れたいときは倍率を 70% 前後に下げる。
- 画像の並べ方は「1列（大きく）」「2列」を切り替えられる。

### Word / Excel / PDF はデスクトップ版だけ

これらは Electron のメインプロセス経由でファイルを保存するため、
**`index.html` をブラウザで開いただけでは出力ボタン自体が表示されない**。
ユーザーがブラウザ版を使っているなら、デスクトップ版（インストーラ）の導入を案内する。

---

## 3. このツールにできないこと（AI が誤解しやすい点）

- **CheckListMaker は MCP サーバーではない。** MCP のツール定義も、CLI も、
  インポート可能なライブラリ API も存在しない。`main.js` は起動引数を一切見ず、
  ウィンドウを開いて `index.html` を読むだけの Electron GUI アプリ
- **AI が .docx / .xlsx / .pdf を直接生成することはできない。** 出力はすべて
  ネイティブの保存ダイアログを通る。AI にできるのは **JSON と取り込み用 HTML** まで
- **AI が「編集できる自己完結 HTML」を作ることはできない。** それには `index.html`
  （約430KB のアプリ本体）を丸ごと埋め込む必要がある。AI が出せるのは
  `clm-data` を持つ閲覧専用の HTML まで
- **AI がスクリーンショットを撮ることはできない。** 録画機能は OS のグローバル入力フックと
  画面キャプチャを使うデスクトップ機能で、ユーザーが手元で実行するもの
- **ヘッドレス／バッチ実行モードは無い**

## 4. 既知の制約

| 制約 | 内容 |
|---|---|
| 共有リンク | 画像は含まれない（URL 長の都合で意図的に落としている） |
| 自己完結 HTML | メモリ上でしか保存しないため、**リロードすると編集が消える**。保存し直すと新しい HTML がダウンロードされる |
| Excel の画像 | 行高の上限 409pt（約545px）に合わせて縮小される。横位置の合わせはピクセル近似で数px ずれることがある |
| Excel の画像形式 | `png` / `jpeg` / `gif` のみ。**webp は無視される** |
| ブラウザ版の保存容量 | localStorage 上限。画像を多く貼ると保存に失敗する（デスクトップ版はファイル保存なので上限が緩い） |
| `type: "todo"` | `note` / `time` / `body` / 表紙が Word・Excel に出ない |
| 配布 .exe | コード署名していないため、ウイルス対策ソフトが警告を出すことがある |

## 5. 出力前にユーザーに確認するとよいこと

- 表紙は要るか（社外・正式文書なら要る）
- 文書番号・版数・作成者・発行日（**推測で埋めない**）
- 標準時間を入れるか（見積りに使うなら必須）
- 出力先は Word か PDF か Excel か
