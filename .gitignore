# 依存パッケージ
node_modules/

# 録画機能が出力するスクリーンショット
# 通常はユーザーの「ピクチャ/CheckListMaker」に保存されるが、ピクチャを取得できない
# 環境ではアプリのあるディレクトリ直下の CheckListMaker/ にフォールバックする。
# 00_スクリーンショット/ は旧バージョンの保存先（後方互換のため残す）。
CheckListMaker/
00_スクリーンショット/

# Electron ビルド成果物
dist/
out/

# OS / エディタ
.DS_Store
Thumbs.db
*.log

# 2-R0 UIA検証スパイクの計測結果（機密になり得るため commit しない）
tools/uia-spike/results-*.jsonl
tools/uia-spike/summary-*.json
tools/uia-spike/package-lock.json
